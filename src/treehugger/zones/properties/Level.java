package treehugger.zones.properties;

/**
 * Enum class to represent Level of each zone on the board.
 */
public enum Level {

    UNDEVELOPED,
    DEVELOPMENT_ONE,
    DEVELOPMENT_TWO,
    DEVELOPMENT_THREE,
    MAJOR_DEVELOPMENT;
}
